import { Foot } from "./style";

export function Footer() {
    return (
        <Foot>@node server.js @Redirecionar páginas @Persistir os dados de busca @Pensar quais informações quero que apareça</Foot>
    );
}